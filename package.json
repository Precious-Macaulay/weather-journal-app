{
  "name": "weather-journal-app",
  "version": "1.0.0",
  "description": "This is an asynchronous web app that uses web API and user data to dynamically update the UI",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "macaulay precious",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.0",
    "cors": "^2.8.5",
    "express": "^4.18.1"
  }
}
